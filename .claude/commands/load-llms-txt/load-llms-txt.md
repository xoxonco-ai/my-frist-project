# Load llms.txt Context

If `$ARGUMENTS` is provided, treat it as a URL or local file path and read the `llms.txt` from there.

Otherwise, check if a `llms.txt` file exists in the project root and read it.

Do nothing else and await further instructions.